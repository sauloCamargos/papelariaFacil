import { ScInputDirective } from './sc-input.directive';

describe('ScInputDirective', () => {
  it('should create an instance', () => {
    const directive = new ScInputDirective();
    expect(directive).toBeTruthy();
  });
});
