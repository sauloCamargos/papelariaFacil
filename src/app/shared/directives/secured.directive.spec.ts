import { SecuredDirective } from './secured.directive';

describe('SecuredDirective', () => {
  it('should create an instance', () => {
    const directive = new SecuredDirective();
    expect(directive).toBeTruthy();
  });
});
